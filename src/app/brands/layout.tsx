import { ReactNode } from 'react';

export const metadata = {
  title: 'Brands | eCom Furniture Admin',
  description: 'Manage your product brands.',
};

export default function BrandsLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
