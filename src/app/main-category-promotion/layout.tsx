import { ReactNode } from 'react';

export const metadata = {
  title: 'Sliders | eCom Furniture Admin',
  description: 'Manage your product sliders.',
};

export default function SlidersLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
