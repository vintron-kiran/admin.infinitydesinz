import { ReactNode } from 'react';

export const metadata = {
  title: 'Colors | eCom Furniture Admin',
  description: 'Manage your product colors.',
};

export default function ColorsLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
